import { NgbdateShowPipe } from './ngbdate-show.pipe';

describe('NgbdateShowPipe', () => {
  it('create an instance', () => {
    const pipe = new NgbdateShowPipe();
    expect(pipe).toBeTruthy();
  });
});
